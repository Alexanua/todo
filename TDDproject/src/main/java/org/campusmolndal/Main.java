package org.campusmolndal;

import java.time.LocalDate;

public class Main {
    public static void main(String[] args) {

        /*
        UserService userService = new UserService();

        // Skapa en användare och registrera den
        User user = new User(1, "Alex", 36, LocalDate.now());
        userService.registerUser(user);

        // Hämta användarinformation från databasen och skriv ut den
        User retrievedUser = userService.getUser(1);
        if (retrievedUser != null) {
            System.out.println("User ID: " + retrievedUser.getId());
            System.out.println("User Name: " + retrievedUser.getName());
            System.out.println("User Age: " + retrievedUser.getAge());
            System.out.println("User Registration Date: " + retrievedUser.getRegisterDate());
        } else {
            System.out.println("User not found");
        }
    }

         */


    }}

